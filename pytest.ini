[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Golden realm tests auto-skip unless a realm is found; point at one explicitly with:
#   ARMADA_TEST_REALM=D:\Work\Hand-realm  (or export it)
